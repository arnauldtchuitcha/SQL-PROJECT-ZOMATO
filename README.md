# SQL-PROJECT-ZOMATO
In this project, we created database with different tables, Primary and foreign keys, data model to see the different relationship between tables. Then, we verified each datatype, cleanned data and did EDA (for example the top 5 most frequently ordered dishes by customer 'Arjun Mehta' in the last 3 year)
